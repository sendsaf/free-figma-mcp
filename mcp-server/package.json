{
  "name": "figma-local-mcp-server",
  "version": "0.1.0",
  "description": "Figma Local MCP server for inspecting and editing Figma Desktop through a local plugin bridge",
  "type": "module",
  "main": "server.js",
  "license": "MIT",
  "keywords": [
    "mcp",
    "figma",
    "figma-plugin",
    "design-to-code",
    "kiro"
  ],
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "figma-local-mcp": "./server.js"
  },
  "files": [
    "server.js",
    "src"
  ],
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check src/api-catalog.js && node --check src/bridge.js && node --check src/capabilities.js && node --check src/code-connect-store.js && node --check src/config.js && node --check src/content.js && node --check src/guidance.js && node --check src/preset-store.js && node --check src/schema-probe.js && node --check src/tools.js",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "ws": "^8.0.0",
    "zod": "^3.0.0"
  }
}
